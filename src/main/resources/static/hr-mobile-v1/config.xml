<?xml version="1.0" encoding="UTF-8"?>
<widget xmlns="http://www.w3.org/ns/widgets"
        xmlns:android="http://schemas.android.com/apk/res/android"
        id="com.yourcompany.justhumanresource"
        version="1.0.0">

    <name>JustHumanResource Employee</name>
    <description>JustHumanResource Mobile App</description>

    <author email="your@email.com" href="https://yourwebsite.com">
        Your Name
    </author>

    <!-- Load local bootstrap page -->
    <content src="index.html" />

    <!-- Network permissions -->
    <access origin="*" />
    <allow-navigation href="*" />
    <allow-intent href="https://*/*" />
    <allow-intent href="http://*/*" />
    <allow-intent href="market:*" />

    <!-- App behaviour -->
    <preference name="orientation" value="portrait" />
    <preference name="fullscreen" value="false" />
    <preference name="AndroidLaunchMode" value="singleTask" />

    <!-- Android SDK -->
    <preference name="android-minSdkVersion" value="23" />
    <preference name="android-targetSdkVersion" value="36" />
    <preference name="android-compileSdkVersion" value="36" />

    <!-- Status bar -->
    <preference name="StatusBarOverlaysWebView" value="false" />
    <preference name="StatusBarBackgroundColor" value="#ffffff" />
    <preference name="StatusBarStyle" value="dark" />

    <!-- Splash -->
    <preference name="AndroidWindowSplashScreenBackground" value="#ffffff" />
    <preference name="AndroidWindowSplashScreenAnimatedIcon"
                value="res/icon/android/icon.png" />
    <preference name="AndroidWindowSplashScreenIconBackgroundColor"
                value="#E9F3FB" />

    <!-- Required plugins -->
    <plugin name="cordova-plugin-statusbar" />
    <plugin name="cordova-plugin-fingerprint-aio" spec="^6.0.0" />
    <plugin name="cordova-plugin-secure-storage-echo" />

    <platform name="android">

        <!-- Allow biometric hardware -->
        <config-file target="AndroidManifest.xml" parent="/manifest">

            <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
            <uses-permission android:name="android.permission.USE_FINGERPRINT"/>

        </config-file>

        <icon src="res/icon/android/icon.png" />

    </platform>

</widget>
